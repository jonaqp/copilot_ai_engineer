PROGRAM BALQRY
INPUT ACCOUNT-RECORD
OUTPUT QUERY-RESPONSE
BEGIN
  IF ACCT-ID = SPACES THEN
    SET RETURN-CODE = 4
    SET MESSAGE = "MISSING ACCOUNT"
  ELSE
    SET RESPONSE-BALANCE = ACCT-BALANCE
    SET RETURN-CODE = 0
    SET MESSAGE = "OK"
  END-IF
END
