package {{PACKAGE}};

/**
 * Esqueleto neutro para demostrar renderizado de una plantilla aprobada.
 * Sustituir esta plantilla por el patron APX homologado del repositorio.
 */
public class {{COMPONENT_NAME}} {
    public String execute(String input) {
        return input;
    }
}
