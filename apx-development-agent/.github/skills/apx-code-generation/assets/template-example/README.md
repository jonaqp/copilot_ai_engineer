# Plantilla de ejemplo NO productiva

Este recurso demuestra el motor de sustitucion. No contiene APIs propietarias APX y **no debe considerarse una plantilla APX homologada**. En un repositorio real, pasar a `--template-dir` una plantilla aprobada recuperada/validada mediante GitHub MCP o el repositorio corporativo.
