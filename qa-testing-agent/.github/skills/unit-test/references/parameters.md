# Parámetros

```bash
python scripts/detect_and_run_tests.py <repo> [--run] [--timeout 120]
```

- `<repo>`: raíz local del proyecto.
- `--run`: ejecutar el comando detectado solo si está en la allowlist del script.
- `--timeout`: segundos máximos.
